{
  "name": "portal_sdk",
  "version": "0.0.1",
  "type": "module",
  "devDependencies": {
    "@types/node": "20.6.0",
    "ts-bundler": "^0.0.2",
    "typescript": "^5.3.3",
    "vite": "^7.1.10"
  },
  "description": "Portal SDK TypeScript bundler",
  "scripts": {
    "build": "vite build",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "typecheck": "tsc --noEmit"
  }
}
